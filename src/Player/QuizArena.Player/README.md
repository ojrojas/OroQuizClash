# QuizArena.Player — Angular 22

Player Experience (Game Show) — consumes `design-system/tokens/design-tokens.css` via CSS variables.

Spec: `specs/016-ui-ux-design-system/` → `design-system/MASTER.md` + `design-system/overrides/player.md`

Pending SPEC-027+ for implementation. Placeholder per tasks.md T004.
