# Page Override: question-bank

> Overrides `design-system/MASTER.md` for `/admin/questions`. Only deviations listed.

- Editor is a right Drawer (400px) not a separate route — deviation for fast bulk editing; 375 → full-screen sheet.
- Correct option marked with radio + `check` icon + text "Correct" (never color-only).
- Options inputs fixed to exactly 4 (no add/remove) per SPEC-003 — UI enforces domain rule.
- "In use by live game" lock shows Badge + read-only drawer (inputs disabled, save hidden).
