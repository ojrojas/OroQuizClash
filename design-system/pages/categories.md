# Page Override: categories

> Overrides `design-system/MASTER.md` for `/admin/categories`. Only deviations listed.

- Status transition Draft→Published animates Badge swap with fade 200 (feedback for publish action).
- Question count column right-aligned tabular-nums (`Fira Code`).
- Archive action hidden for categories with published games (disabled + tooltip reason) — deviation: action availability is data-driven.
