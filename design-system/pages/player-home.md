# Page Override: player-home

> Overrides `design-system/MASTER.md` for `/` (player). Only deviations listed.

- Hero uses `--typography-display-size` Russo One with subtle radial glow bg (elevation, not glassmorphism) — allowed cinematic effect.
- Featured carousel auto-advances 6s; pauses on hover/focus/reduced-motion; arrows + dots both keyboard operable.
- Bottom nav fixed@375–768 with safe-area padding; hidden@1024+ (top links instead).
